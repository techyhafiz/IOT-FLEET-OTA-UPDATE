# Progress Log — IoT OTA Fleet Management Dashboard
# Branch: ralph/iot-ota-dashboard
# Started: 2026-09-22

## Codebase Patterns
<!-- Add reusable patterns here as they are discovered during implementation -->

- **API Base URL:** Always read from `import.meta.env.VITE_API_BASE` — never hardcode
- **WebSocket URL:** Derive from VITE_API_BASE by replacing `http` with `ws`
- **Shared components:** Live in `shared/components/`, imported via `../../shared/components/ComponentName`
- **Shared types:** Live in `shared/types.ts`, imported via `../../shared/types`
- **Dark theme tokens:** bg=#0f172a, cyan=#06b6d4, green=#22c55e, amber=#f59e0b, red=#ef4444, slate-800=#1e293b, slate-700=#334155
- **CORS:** FastAPI CORSMiddleware with allow_origins=["*"] for demo simplicity
- **In-memory state:** Backend uses a plain Python dict `devices: dict[str, Device]` — no DB
- **OTA simulation:** Simulator sends ota_progress 0→100 in 10% increments via setInterval, then posts state=complete

---

## 2026-09-22 19:30 - US-001: Project Scaffold & Monorepo Setup
- Created: backend/main.py (FastAPI stub), backend/requirements.txt, backend/firmware/ (3 placeholder .bin files)
- Created: dashboard/ and simulator/ via `npm create vite@latest` (react-ts template)
- Installed: tailwindcss @tailwindcss/vite react-syntax-highlighter in both frontends
- Created: shared/types.ts with Device, LogEntry, FirmwareVersion, Group, OTAStatus, WSEvent interfaces
- Configured: vite.config.ts (ports 5173 / 3000, @shared alias), tsconfig.app.json (@shared paths), index.css (Tailwind v4 + custom animations)
- Created: .env / .env.example for both frontends, README.md, AGENTS.md
- Files changed: 20+ files across all 4 directories
- **Learnings for future iterations:**
  - Patterns: Use `@shared/types` import alias in both frontends — works via vite resolve.alias + tsconfig paths
  - Gotchas: Tailwind v4 uses `@import "tailwindcss"` not `@tailwind base/components/utilities`
  - Gotchas: PowerShell doesn't support `&&` — use separate commands
  - Useful: All Python deps (fastapi, uvicorn, websockets) were already installed globally
---
